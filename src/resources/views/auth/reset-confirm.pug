html
    head
        meta(charset='UTF-8')
        meta(http-equiv='X-UA-Compatible', content='IE=edge')
        meta(name='viewport', content='width=device-width, initial-scale=1.0')
        title Đặt lại mật khẩu
        link(href='https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap', rel='stylesheet')
        link(href='https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css', rel='stylesheet', integrity='sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC', crossorigin='anonymous')
        link(rel='stylesheet', href='/css/app.css')
        link(rel="stylesheet", href="/vendor/eggy-js/css/eggy.css")
        link(rel="stylesheet", href="/vendor/eggy-js/css/progressbar.css")
        link(rel="stylesheet", href="/vendor/eggy-js/css/theme.css")
        
    body
        .container-fluid.background
            .row.align-items-center.justify-content-center.h-100
                .col-md-4.row-container
                    .text-center.mb-5
                        h2.fw-bold Đặt lại mật khẩu
                        p.mt-4.fs-6(style="line-height: 1.5") Nhập mật khẩu mới để đặt lại mật khẩu cho tài khoản của bạn.
                    form(action=`/reset/confirm/${token}`, method='POST')
                        .form-field.mb-4
                            input.form-input(placeholder=' ', type='password', name='password', value=values ? values.password : '', autocomplete="off", maxlength="30")
                            label.form-label Mật khẩu mới
                        .form-field.mb-4
                            input.form-input(placeholder=' ', type='password', name='passwordConfirm', value=values ? values.passwordConfirm : '', autocomplete="off", maxlength="30")
                            label.form-label Xác nhận mật khẩu mới
                        button.btn.btn-success.mx-auto.d-block(type='submit') Đặt lại mật khẩu
                        div.text-center.mt-5.fs-6
                            | Bạn đã có tài khoản? 
                            a(href='/login').text-primary Đăng nhập
                            div.mt-2
                                a(href='/').fs-7 Trang chủ
        if errors.length > 0
            - var msgError = errors
        else if success.length > 0
            - var msgSuccess = success
        script(src="/vendor/eggy-js/js/eggy.js")
        script.
            // toast message
            let msgToastError = '#{msgError}';
            let msgToastSuccess = '#{msgSuccess}';

            if(msgToastError != ''){
                Eggy({
                    title: 'Oops... Có gì đó không đúng',
                    message:  msgToastError,
                    type: 'warning',
                    duration: 5000,
                });
            }

            if(msgToastSuccess != ''){
                Eggy({
                    title: 'Thành công',
                    message:  msgToastSuccess,
                    type: 'success',
                    duration: 5000,
                });
            }
